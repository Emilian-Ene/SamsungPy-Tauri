name: Build Windows EXE

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: SamsungPy.v3/tauri-app/package-lock.json

      - name: Install frontend dependencies
        working-directory: SamsungPy.v3/tauri-app
        run: npm ci

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: SamsungPy.v3/tauri-app
          args: --target x86_64-pc-windows-msvc

      - name: Upload Windows bundle artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-bundle
          path: |
            SamsungPy.v3/tauri-app/src-tauri/target/x86_64-pc-windows-msvc/release/bundle/**
            SamsungPy.v3/tauri-app/src-tauri/target/release/bundle/**
          if-no-files-found: error
